import { TypeActions } from '../action';

import styles from './ActionButton.module.scss';

function ActionButton({ data }: TypeActions) {
    return (
        <div>
            
        </div>
    );
};

export { ActionButton };
